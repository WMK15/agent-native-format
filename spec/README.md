# Specification workspace

This directory is intentionally a requirements workspace, not a file-format specification.

Requirements enter here only when benchmark results support them. Each requirement should link to the experiment, dataset/task versions, raw results, and analysis that justify it. A future syntax proposal must satisfy these criteria and compete with the established baselines under the same protocol.

Until then, there is no canonical agent-native extension, grammar, MIME type, or parser.
